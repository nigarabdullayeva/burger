<li>
	{{burger_name}}

    
	<button class="btn btn-outline-warning mb-2 devour" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}} Devour It!{{else}} Not devour!{{/if}} </button>

</li>